# Testing

- Unit tests: JUnit 4, MockK, Turbine, kotlinx-coroutines-test.
- UI tests: Kaspresso with Kakao Compose screen objects, network stubbed with MockWebServer.
- CI runs the smoke suite through Marathon on the nightly schedule.
